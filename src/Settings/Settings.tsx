import React from 'react';
import {SafeAreaView, Text, View} from 'react-native';

export const Settings = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>Settings</Text>
      </View>
    </SafeAreaView>
  );
};
